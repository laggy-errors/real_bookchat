// TODO: Implement user profiles, display settings, avatars, and reading statistics models.
export interface UserStats {
  booksRead: number;
  totalReadingMinutes: number;
  currentStreakDays: number;
}
